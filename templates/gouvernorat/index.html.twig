{% extends "admin/bars.html.twig" %}

{% block contenpage %}
<div class="">
    <div class="page-title">
        <div class="title_left">

        </div>
    </div>

    <div class="clearfix"></div>

    <div class="row">
        <div class="col-md-12">
            <div class="x_panel">
                <div class="x_title">
                    <h2>
                        Listes des gouvernorats
                    </h2>
                    <ul class="nav navbar-right panel_toolbox">
                        <li>
                            <a href="{{path('app_gouvernorat_new')}}" class="btn btn-success">Nouvelle gouvernorat</a>
                        </li>
                    </ul>
                    <div class="clearfix"></div>
                </div>
                <div class="x_content">
                    <div class="row">

                        <table id="example" class="display table table-striped table-bordered dataminetables">
        <thead>
            <tr>
                <th>Id</th>
                <th>Identifiant</th>
                <th>Libelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for gouvernorat in gouvernorats %}
            <tr>
                <td>{{ gouvernorat.id }}</td>
                <td>{{ gouvernorat.identifiant }}</td>
                <td>{{ gouvernorat.libelle }}</td>
                <td>
                    <a href="{{ path('app_gouvernorat_show', {'id': gouvernorat.id}) }}" class="btn btn-primary"><i class="fa fa-eye"></i></a>
                    <a href="{{ path('app_gouvernorat_edit', {'id': gouvernorat.id}) }}" class="btn btn-warning"><i class="fa fa-edit"></i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
</div>
</div>
</div>
</div>
</div>
{% endblock %}

    
